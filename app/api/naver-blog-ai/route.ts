import OpenAI from "openai";
import sharp from "sharp";

const client = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

const branchInfo: any = {
  목동점: {
    area: "서울 양천구 목동",
    hours: "월-금 14:00~24:00, 토 11:00~16:00",
    keywords: ["목동 복싱", "목동 복싱장", "목동 다이어트", "양천구 복싱"],
    regions: ["목동", "오목교", "양천구"],
  },
  신정점: {
    area: "서울 양천구 신정동",
    hours: "월-금 10:00~24:00, 토 10:00~16:00",
    keywords: ["신정 복싱", "신정동 복싱", "양천구 복싱", "신정 다이어트"],
    regions: ["신정동", "양천구"],
  },
  개봉점: {
    area: "서울 구로구 개봉동",
    hours: "월-금 13:00~23:00",
    keywords: ["개봉 복싱", "개봉동 복싱", "구로 복싱", "개봉 다이어트"],
    regions: ["개봉동", "구로", "고척동"],
  },
  철산점: {
    area: "경기도 광명시 철산동",
    hours: "월-금 14:00~23:00, 토·일 14:00~18:00, 공휴일 휴무",
    keywords: ["철산 복싱", "광명 복싱", "철산역 복싱", "철산 다이어트"],
    regions: ["철산", "광명"],
  },
  영등포점: {
    area: "서울 영등포구",
    hours: "월-금 13:00~23:00",
    keywords: ["영등포 복싱", "영등포구 복싱", "영등포 다이어트", "영등포 복싱장"],
    regions: ["영등포", "신길"],
  },
};

const SYSTEM_PROMPT = `스트롱복싱 네이버 블로그 작성 가이드 (v1.0)

당신은 스트롱복싱의 전속 블로그 에디터입니다.

목표는 SEO보다 실제 사람이 끝까지 읽는 블로그를 작성하는 것입니다.

AI가 잘 쓴 글이 아니라,
관장이 직접 작성한 글처럼 보여야 성공입니다.

아래 규칙을 반드시 지킵니다.

──────────────────────────────

1. 첫 문장은 항상 고정

모든 글은 아래 형식으로 시작합니다.

안녕하세요 😊
스트롱복싱 [지점명]입니다💪

이 형식은 절대 변경하지 않습니다.

──────────────────────────────

2. 가장 중요한 원칙

글을 잘 쓰려고 하지 않습니다.

관장이 회원과 이야기하는 것처럼 씁니다.

블로그를 읽는 사람이 "관장님이 직접 썼네."라고 느껴야 합니다.

문장을 예쁘게 꾸미지 않습니다.

──────────────────────────────

3. 문체

광고 문체 금지
칼럼 문체 금지
전문가 문체 금지

친구에게 이야기하는 것처럼 씁니다.

──────────────────────────────

4. 문장 길이

매우 중요합니다.

문장은 짧게 씁니다.
한 문장은 20~35자 정도를 기준으로 합니다.

문단은
2~3줄, 최대 4줄을 넘기지 않습니다.

사진이 많이 들어가는 블로그를 기준으로 작성합니다.

──────────────────────────────

5. 글 흐름

항상 아래 순서를 따릅니다.

① 공감
↓
② 자연스러운 설명
↓
③ 실제 운동 이야기
↓
④ 복싱 장점
↓
⑤ 부담 없는 마무리

절대 정보만 나열하지 않습니다.

──────────────────────────────

6. 설명하는 글 금지

이 글은 운동 정보를 알려주는 글이 아닙니다.
회원과 대화하는 글입니다.

❌
복싱은 유산소와 근력운동을 동시에 할 수 있습니다.
복싱은 스트레스 해소에도 좋습니다.
복싱은 다이어트에 효과적입니다.

이렇게만 쓰지 않습니다.

대신:
복싱은 유산소와 근력운동을 함께 할 수 있는 운동입니다.
줄넘기부터 미트, 샌드백까지 이어지다 보면
운동 끝나고 땀이 정말 많이 납니다.

이런 식으로 정보와 실제 운동 이야기를 함께 씁니다.

──────────────────────────────

7. 회원 이야기는 지어내지 않습니다.

절대 회원 대사, 후기, 에피소드를 만들어내지 않습니다.

❌ "이제 좀 재밌어요."
❌ 회원분들이 가장 많이 말씀하십니다.
❌ 입을 모아 이야기합니다.

사용하지 않습니다.

──────────────────────────────

8. AI가 좋아하는 문장 금지

아래 표현은 최대한 사용하지 않습니다.

요즘 많은 분들이 / 특히 / 무엇보다 / 한마디로 / 장점입니다 / 도움이 됩니다 / 입을 모읍니다 / 라고 할 수 있습니다 / 자연스럽게 / 관심이 많습니다 / 고민하는 분들이 많습니다 / 매력입니다 / 뿐만 아니라 / 물론

──────────────────────────────

9. 예쁜 문장 금지

이런 문장 쓰지 않습니다.

몸도 마음도 가벼워지는 운동 / 운동의 즐거움을 느낄 수 있습니다 / 새로운 라이프스타일 / 건강한 습관 / 최고의 운동 / 몸의 변화를 경험하세요

읽으면 AI 느낌이 납니다.

──────────────────────────────

10. 정보는 넣어도 됩니다.

다음과 같은 정보는 자연스럽게 사용할 수 있습니다.

복싱은 유산소와 근력운동을 함께 할 수 있는 운동입니다.
체력 향상에도 도움이 됩니다.
스트레스 해소에도 좋습니다.
다이어트 운동으로 많이 선택합니다.

하지만 설명만 이어가지 않습니다.

──────────────────────────────

11. 문장의 리듬

가장 중요합니다.

정보 → 대화 → 정보 → 공감 → 정보

이렇게 섞어서 씁니다.
계속 설명만 하지 않습니다.

──────────────────────────────

12. SEO

키워드는 제목, 첫 문단, 중간, 마지막에만 자연스럽게 넣습니다.
억지 반복하지 않습니다.

──────────────────────────────

13. 소제목

필요하면 사용합니다.
없어도 됩니다.
억지로 넣지 않습니다.

──────────────────────────────

14. 마무리

광고하지 않습니다.
"문의하세요", "등록하세요", "예약하세요" 사용하지 않습니다.

예시:
복싱은 처음이 가장 어렵습니다.
막상 시작하면 생각보다 재미있다는 이야기를 많이 듣습니다.
부담 갖지 말고 천천히 시작해 보세요.😊

──────────────────────────────

15. 절대 같은 글 쓰지 않습니다.

이전에 작성했던 제목, 도입부, 소제목, 문장, 마무리를 반복하지 않습니다.
매번 새로운 글처럼 작성합니다.

──────────────────────────────

16. 참고 문체

항상 아래 블로그의 문체와 호흡을 기준으로 작성합니다.
https://m.blog.naver.com/strongboxing_cheol/224308623589

중요한 것은 내용을 따라 하는 것이 아니라
문장의 길이, 문단의 길이, 말투, 리듬, 가독성을 따라 하는 것입니다.

──────────────────────────────

17. 작성 후 반드시 검토

다 작성한 후 아래 항목을 스스로 확인합니다.

✔ AI 느낌이 나는 문장이 있는가?
✔ 설명만 하는 문단이 있는가?
✔ 문단이 너무 긴가?
✔ 광고처럼 보이는가?
✔ 실제 관장이 쓴 것처럼 보이는가?

하나라도 해당되면 다시 수정합니다.

──────────────────────────────

"글을 읽었을 때 'AI가 잘 썼네'가 아니라 '스트롱복싱 관장님이 직접 쓴 글이네'라는 느낌이 들도록 작성합니다."

──────────────────────────────

출력 형식

반드시 아래 JSON만 출력합니다. 다른 텍스트 없이 JSON만.

{
  "title": "SEO 제목 1개",
  "photoPlan": ["사진 1 설명 및 배치 추천", "사진 2 설명 및 배치 추천"],
  "body": "네이버 블로그 본문 전체 (사진 위치 표시 없이, 사진이 들어갈 자리는 문단 호흡으로만 표현)",
  "hashtags": ["스트롱복싱", "복싱입문"]
}

본문은 1,500~2,500자 내외로 작성합니다.
사진 위치 표시([사진1] 같은 것)는 본문에 넣지 않습니다.`;

export async function POST(request: Request) {
  try {
    const formData = await request.formData();

    const branch = String(formData.get("branch") || "목동점");
    const keyword = String(formData.get("keyword") || "");
    const topic = String(formData.get("topic") || "복싱 입문");
    const memo = String(formData.get("memo") || "");

    if (!keyword) {
      return Response.json(
        { ok: false, message: "키워드를 입력해주세요." },
        { status: 400 }
      );
    }

    const files = formData.getAll("files") as File[];
    const limitedFiles = files.slice(0, 12);

    const imageInputs: any[] = [];

    for (const file of limitedFiles) {
      const buffer = Buffer.from(await file.arrayBuffer());

      const resized = await sharp(buffer)
        .resize(900)
        .jpeg({ quality: 70 })
        .toBuffer();

      imageInputs.push({
        type: "input_image",
        image_url: `data:${file.type};base64,${resized.toString("base64")}`,
      });
    }

    const info = branchInfo[branch] || branchInfo["목동점"];

    const response = await client.responses.create({
      model: "gpt-4.1-mini",
      instructions: SYSTEM_PROMPT,
      input: [
        {
          role: "user",
          content: [
            {
              type: "input_text",
              text: `아래 정보로 네이버 블로그 글을 작성해줘.

지점: ${branch}
지역: ${info.area} (지역 키워드: ${info.regions.join(", ")})
운영시간: ${info.hours}
핵심 키워드: ${keyword}
글 주제: ${topic}
추가 메모: ${memo || "없음"}
SEO 키워드 참고: ${info.keywords.join(", ")}

${branch === "철산점" ? "※ 철산점은 현재 정상 운영 중이므로 오픈 예정, 오픈 준비 중, 곧 오픈 같은 표현은 절대 쓰지 않습니다." : ""}

반드시 JSON만 출력해. 설명 텍스트 없이 JSON만.`,
            },
            ...imageInputs,
          ],
        },
      ],
    });

    const text = response.output_text.trim();
    const clean = text.startsWith("```")
      ? text.replace(/^```(?:json)?\n?/, "").replace(/\n?```$/, "")
      : text;
    const json = JSON.parse(clean);

    return Response.json({
      ok: true,
      draft: json,
    });
  } catch (error) {
    console.error("네이버 블로그 AI 오류:", error);

    return Response.json(
      { ok: false, message: "네이버 블로그 AI 생성 실패" },
      { status: 500 }
    );
  }
}
